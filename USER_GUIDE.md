# Cinema Scheduler System 前端使用說明

## 1. 系統簡介

Cinema Scheduler System 是一套影院員工排班管理系統，提供排班管理者管理員工資料、可排班時段、職位、週排班、班表指派、工時統計，以及人力不足與人力過多檢查。

本系統為單一使用者的內部管理工具，主要用於協助影院排班管理者建立、檢查與調整每週班表。

## 2. 系統畫面

系統採用 dashboard 式版面，主要分為上方標題列、左側選單與右側內容區。

### 上方標題列

上方標題列會顯示系統名稱：

```text
Cinema Scheduler System
```

### 左側選單

左側選單提供以下功能：

1. Employee Management
2. Availability Management
3. Position Management
4. Weekly Schedule Management
5. Work Hour Statistics
6. Understaffing Check
7. Overstaffing Check

點選選單項目後，右側內容區會切換到對應管理頁面。

## 3. 建議使用流程

建議依照以下順序使用系統：

1. 建立或確認員工資料
2. 設定員工可排班時段
3. 確認職位資料
4. 建立週排班
5. 指派員工到指定日期、職位與時段
6. 查看員工工時統計
7. 執行人力不足檢查
8. 執行人力過多檢查
9. 根據檢查結果調整班表

## 4. Employee Management 員工管理

員工管理頁面用於維護影院員工資料。

### 功能

- 查看員工列表
- 新增員工
- 編輯員工資料
- 刪除員工

### 員工資料欄位

| 欄位 | 說明 |
| --- | --- |
| id | 員工 ID |
| name | 員工姓名 |
| jobTitle | 職稱 |
| isActive | 是否啟用 |
| note | 備註 |
| createdAt | 建立時間 |
| updatedAt | 更新時間 |

### 操作說明

在員工列表中可以查看目前所有員工。若要新增員工，點選新增功能並填寫姓名、職稱、啟用狀態與備註。

若員工資料需要修改，可使用編輯功能更新資料。若員工不再需要保留於系統中，可使用刪除功能移除。

## 5. Availability Management 可排班時段管理

可排班時段管理頁面用於設定員工每週可以工作的時間。

### 功能

- 查看所有可排班時段
- 查看指定員工的可排班時段
- 新增可排班時段
- 編輯可排班時段
- 刪除可排班時段

### 可排班資料欄位

| 欄位 | 說明 |
| --- | --- |
| id | 可排班時段 ID |
| employee | 員工 |
| weekday | 星期 |
| startTime | 開始時間 |
| endTime | 結束時間 |

### 操作說明

管理者可以為每位員工設定可工作的星期與時間區間。

範例：

| 員工 | 星期 | 開始時間 | 結束時間 |
| --- | --- | --- | --- |
| 晚班A | Friday | 17:00 | 23:00 |

可排班資料可作為後續安排班表時的參考依據。

## 6. Position Management 職位管理

職位管理頁面用於維護影院內的工作職位。

### 功能

- 查看職位列表
- 新增職位
- 編輯職位
- 刪除職位

### 職位資料欄位

| 欄位 | 說明 |
| --- | --- |
| id | 職位 ID |
| name | 職位名稱 |
| isRequired | 是否為必要職位 |
| createdAt | 建立時間 |
| updatedAt | 更新時間 |

### 目前職位

| ID | 職位名稱 | 類型 |
| --- | --- | --- |
| 8 | 1F+公關 | 必要職位 |
| 9 | 3F | 必要職位 |
| 10 | 票房 | 必要職位 |
| 11 | 販賣 | 必要職位 |
| 12 | 開機+輪休 | 必要職位 |
| 13 | 關機+輪休 | 必要職位 |
| 14 | 票+販 | 可選職位 |

## 7. Weekly Schedule Management 週排班管理

週排班管理頁面用於建立與管理每一週的排班表。

### 功能

- 查看週排班列表
- 新增週排班
- 編輯週排班
- 刪除週排班

### 週排班資料欄位

| 欄位 | 說明 |
| --- | --- |
| id | 週排班 ID |
| startDate | 開始日期 |
| endDate | 結束日期 |
| status | 狀態 |

### 操作範例

建立一筆週排班：

| 欄位 | 範例 |
| --- | --- |
| startDate | 2026-04-27 |
| endDate | 2026-05-03 |
| status | DRAFT |

建立週排班後，即可針對該週安排員工班表。

## 8. Schedule Assignment 班表指派

班表指派功能用於將員工安排到指定日期、職位與工作時段。

### 班表指派欄位

| 欄位 | 說明 |
| --- | --- |
| id | 班表指派 ID |
| date | 日期 |
| employee | 員工 |
| position | 職位 |
| startTime | 開始時間 |
| endTime | 結束時間 |
| note | 備註 |

### 操作範例

| 欄位 | 範例 |
| --- | --- |
| date | 2026-05-19 |
| employee | 晚班A |
| position | 1F+公關 |
| startTime | 16:50 |
| endTime | 23:00 |
| note | 晚班 |

### 排班限制

同一位員工不可被安排到時間重疊的班次。

若新班次與既有班次符合以下條件，即代表發生排班衝突：

```text
新班次開始時間 < 既有班次結束時間
且
新班次結束時間 > 既有班次開始時間
```

多位員工可以被安排到同一個職位。例如「販賣」職位可以同時安排多位員工，這是允許的。

## 9. Work Hour Statistics 工時統計

工時統計頁面用於查看員工的工作時數摘要。

### 顯示規則

工時會以 0.5 小時為單位顯示，並四捨五入到最接近的 0.5 小時。

### 休息扣除規則

| 連續工作時間 | 扣除休息時間 |
| --- | --- |
| 小於 4 小時 | 不扣除 |
| 大於或等於 4 小時 | 扣除 30 分鐘 |

### 範例

班次時間：

```text
17:20 - 23:00
```

原始時長為 5 小時 40 分鐘。因為連續工作時間大於 4 小時，所以扣除 30 分鐘休息時間後，顯示為：

```text
5.0 小時
```

## 10. Understaffing Check 人力不足檢查

人力不足檢查頁面用於查看後端提供的人力不足檢查結果。

### 顯示內容

- 發生人力不足的日期
- 發生人力不足的職位
- 缺少人力的時間區間
- 缺少人數

### 操作說明

管理者可透過此頁面確認哪些時段尚未排足人力，並回到班表指派功能補足班次。

人力不足的計算由後端完成，前端只負責顯示結果。

## 11. Overstaffing Check 人力過多檢查

人力過多檢查頁面用於查看後端提供的人力過多檢查結果。

### 顯示內容

- 發生人力過多的日期
- 發生人力過多的職位
- 人力過多的時間區間
- 超出人數

### 操作說明

管理者可透過此頁面檢查是否有職位安排了過多員工，並視情況調整班表。

人力過多的計算由後端完成，前端只負責顯示結果。

## 12. 注意事項

- 員工不可被安排到時間重疊的班次。
- 同一個職位可以安排多位員工。
- 人力不足與人力過多檢查由後端計算。
- 前端只需要顯示檢查結果，不需要自行計算人力缺口或超額。
- 工時會以 0.5 小時為單位顯示。
- 工作時間滿 4 小時會扣除 30 分鐘休息時間。
- 建議每次完成班表調整後，重新查看工時統計、人力不足檢查與人力過多檢查。

